// Generated file, do not edit
#include "example_shaders.h"
namespace resources::shaders {
  ::visage::EmbeddedFile fileByName(const std::string& filename) {
    if (filename == "fs_crt")
      return fs_crt;
    if (filename == "fs_glitch")
      return fs_glitch;
    if (filename == "fs_gray_scale")
      return fs_gray_scale;
    if (filename == "fs_overlay")
      return fs_overlay;
    if (filename == "fs_sepia")
      return fs_sepia;
    if (filename == "fs_shader_quad")
      return fs_shader_quad;
    if (filename == "fs_warp")
      return fs_warp;
    if (filename == "vs_custom")
      return vs_custom;
    if (filename == "vs_overlay")
      return vs_overlay;
    if (filename == "vs_shader_quad")
      return vs_shader_quad;
    if (filename == "varying_def_sc")
      return varying_def_sc;
    if (filename == "fs_crt_sc")
      return fs_crt_sc;
    if (filename == "fs_glitch_sc")
      return fs_glitch_sc;
    if (filename == "fs_gray_scale_sc")
      return fs_gray_scale_sc;
    if (filename == "fs_overlay_sc")
      return fs_overlay_sc;
    if (filename == "fs_sepia_sc")
      return fs_sepia_sc;
    if (filename == "fs_shader_quad_sc")
      return fs_shader_quad_sc;
    if (filename == "fs_warp_sc")
      return fs_warp_sc;
    if (filename == "vs_custom_sc")
      return vs_custom_sc;
    if (filename == "vs_overlay_sc")
      return vs_overlay_sc;
    if (filename == "vs_shader_quad_sc")
      return vs_shader_quad_sc;
    return { nullptr, 0 };
  }
}
